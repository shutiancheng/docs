---
title: "GET /api/webhooks/twilio/voice"
description: "* GET /api/webhooks/twilio/voice * Health check endpoint."
api: "GET /api/webhooks/twilio/voice"
---

* GET /api/webhooks/twilio/voice * Health check endpoint.



## Response

Returns `unknown` on success.

## Example

<CodeGroup>

```bash cURL
curl -X GET https://your-domain.com/api/webhooks/twilio/voice \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```typescript TypeScript
const response = await fetch('/api/webhooks/twilio/voice', {
  method: 'GET',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`
  }
});
const data = await response.json();
```

</CodeGroup>

---
title: "CustomerDetailSkeleton"
description: "* SkeletonHeader - Skeleton for customer detail header /
function SkeletonHeader() {
  return (
    <header
      className="flex items-center justify-between"
      style={{ marginBottom: "var(--space-5)" }}
    >
      <div className="flex items-center gap-4">
        {/* Back button */}
        <Skeleton
          style={{
            width: "var(--detail-back-btn-size)",
            height: "var(--detail-back-btn-size)",
            borderRadius: 8,
          }}
        />
        <div>
          {/* Name + badges row */}
          <div className="flex items-center gap-3">
            <Skeleton style={{ width: 150, height: 18 }} />
            {/* Status badge */}
            <Skeleton style={{ width: 55, height: 22, borderRadius: 9999 }} />
          </div>
          {/* Type · postcode subtitle */}
          <Skeleton style={{ width: 140, height: 14, marginTop: 6 }} />
        </div>
      </div>
      {/* Edit button */}
      <Skeleton style={{ width: 60, height: 32, borderRadius: 8 }} />
    </header>
  );
}

/** SkeletonQuickContactBar - Skeleton for the quick contact bar /
function SkeletonQuickContactBar() {
  return (
    <div
      className="flex items-center gap-6 p-4 rounded-xl mb-6"
      style={{ background: "var(--bg-secondary)", border: "1px solid var(--border-primary)" }}
    >
      {/* Phone */}
      <div className="flex items-center gap-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 110, height: 16 }} />
      </div>
      {/* Copy button */}
      <Skeleton style={{ width: 50, height: 24, borderRadius: 4 }} />

      {/* Divider */}
      <Skeleton style={{ width: 1, height: 24 }} />

      {/* Email */}
      <div className="flex items-center gap-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 160, height: 16 }} />
      </div>

      {/* Divider */}
      <Skeleton style={{ width: 1, height: 24 }} />

      {/* Address */}
      <div className="flex items-center gap-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 180, height: 16 }} />
      </div>
    </div>
  );
}

/** SkeletonCustomerSidebar - Skeleton for customer detail sidebar /
function SkeletonCustomerSidebar() {
  return (
    <aside
      className="rounded-xl"
      style={{ background: "var(--bg-primary)", border: "1px solid var(--border-primary)" }}
    >
      {/* Customer Info section */}
      <div className="p-4" style={{ borderBottom: "1px solid var(--border-primary)" }}>
        <Skeleton style={{ width: 90, height: 12, marginBottom: 12 }} />

        {/* Avatar + Name */}
        <div className="flex items-center gap-3 mb-4">
          <Skeleton variant="circle" style={{ width: 48, height: 48 }} />
          <div>
            <Skeleton style={{ width: 100, height: 16, marginBottom: 4 }} />
            <Skeleton style={{ width: 70, height: 12 }} />
          </div>
        </div>

        {/* Customer since */}
        <div className="flex items-center gap-2">
          <Skeleton style={{ width: 16, height: 16 }} />
          <Skeleton style={{ width: 140, height: 14 }} />
        </div>
      </div>

      {/* Type & Tags section */}
      <div className="p-4" style={{ borderBottom: "1px solid var(--border-primary)" }}>
        <Skeleton style={{ width: 80, height: 12, marginBottom: 12 }} />

        {/* Type badge */}
        <Skeleton style={{ width: 80, height: 26, borderRadius: 9999, marginBottom: 12 }} />

        {/* Tags */}
        <div className="flex flex-wrap gap-2">
          <Skeleton style={{ width: 60, height: 24, borderRadius: 9999 }} />
          <Skeleton style={{ width: 75, height: 24, borderRadius: 9999 }} />
          <Skeleton style={{ width: 50, height: 24, borderRadius: 9999 }} />
        </div>
      </div>

      {/* Notes Preview section */}
      <div className="p-4">
        <Skeleton style={{ width: 100, height: 12, marginBottom: 8 }} />
        <Skeleton style={{ width: "100%", height: 14, marginBottom: 4 }} />
        <Skeleton style={{ width: "90%", height: 14, marginBottom: 4 }} />
        <Skeleton style={{ width: "70%", height: 14 }} />
      </div>
    </aside>
  );
}

/** SkeletonCallHistory - Skeleton for call history section /
function SkeletonCallHistory() {
  return (
    <SkeletonCard style={{ overflow: "hidden" }}>
      {/* Header */}
      <div
        className="px-4 py-3 border-b flex items-center justify-between"
        style={{ borderColor: "var(--border-primary)" }}
      >
        <Skeleton style={{ width: 80, height: 16 }} />
        <Skeleton style={{ width: 50, height: 14 }} />
      </div>

      {/* Call entries */}
      <div className="p-4">
        {Array.from({ length: 4 }).map((_, i) => (
          <div
            key={i}
            className="flex items-center gap-4 py-3 border-b last:border-b-0"
            style={{ borderColor: "var(--border-primary)" }}
          >
            {/* Icon */}
            <Skeleton style={{ width: 32, height: 32, borderRadius: 8 }} />

            {/* Content */}
            <div className="flex-1">
              <div className="flex items-center gap-2 mb-1">
                <Skeleton style={{ width: 100, height: 14 }} />
                <Skeleton style={{ width: 70, height: 20, borderRadius: 9999 }} />
              </div>
              <Skeleton style={{ width: 180, height: 12 }} />
            </div>

            {/* Date/time */}
            <div className="text-right">
              <Skeleton style={{ width: 60, height: 14, marginLeft: "auto" }} />
              <Skeleton style={{ width: 40, height: 12, marginTop: 4, marginLeft: "auto" }} />
            </div>
          </div>
        ))}
      </div>
    </SkeletonCard>
  );
}

/** SkeletonNotesEditor - Skeleton for notes editor section /
function SkeletonNotesEditor() {
  return (
    <SkeletonCard style={{ padding: 16 }}>
      <Skeleton style={{ width: 100, height: 16, marginBottom: 12 }} />
      <Skeleton
        style={{
          width: "100%",
          height: 120,
          borderRadius: 8,
        }}
      />
    </SkeletonCard>
  );
}

/** CustomerDetailSkeleton - Full customer detail page skeleton"
---

* SkeletonHeader - Skeleton for customer detail header /
function SkeletonHeader() {
  return (
    <header
      className="flex items-center justify-between"
      style={{ marginBottom: "var(--space-5)" }}
    >
      <div className="flex items-center gap-4">
        {/* Back button */}
        <Skeleton
          style={{
            width: "var(--detail-back-btn-size)",
            height: "var(--detail-back-btn-size)",
            borderRadius: 8,
          }}
        />
        <div>
          {/* Name + badges row */}
          <div className="flex items-center gap-3">
            <Skeleton style={{ width: 150, height: 18 }} />
            {/* Status badge */}
            <Skeleton style={{ width: 55, height: 22, borderRadius: 9999 }} />
          </div>
          {/* Type · postcode subtitle */}
          <Skeleton style={{ width: 140, height: 14, marginTop: 6 }} />
        </div>
      </div>
      {/* Edit button */}
      <Skeleton style={{ width: 60, height: 32, borderRadius: 8 }} />
    </header>
  );
}

/** SkeletonQuickContactBar - Skeleton for the quick contact bar /
function SkeletonQuickContactBar() {
  return (
    <div
      className="flex items-center gap-6 p-4 rounded-xl mb-6"
      style={{ background: "var(--bg-secondary)", border: "1px solid var(--border-primary)" }}
    >
      {/* Phone */}
      <div className="flex items-center gap-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 110, height: 16 }} />
      </div>
      {/* Copy button */}
      <Skeleton style={{ width: 50, height: 24, borderRadius: 4 }} />

      {/* Divider */}
      <Skeleton style={{ width: 1, height: 24 }} />

      {/* Email */}
      <div className="flex items-center gap-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 160, height: 16 }} />
      </div>

      {/* Divider */}
      <Skeleton style={{ width: 1, height: 24 }} />

      {/* Address */}
      <div className="flex items-center gap-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 180, height: 16 }} />
      </div>
    </div>
  );
}

/** SkeletonCustomerSidebar - Skeleton for customer detail sidebar /
function SkeletonCustomerSidebar() {
  return (
    <aside
      className="rounded-xl"
      style={{ background: "var(--bg-primary)", border: "1px solid var(--border-primary)" }}
    >
      {/* Customer Info section */}
      <div className="p-4" style={{ borderBottom: "1px solid var(--border-primary)" }}>
        <Skeleton style={{ width: 90, height: 12, marginBottom: 12 }} />

        {/* Avatar + Name */}
        <div className="flex items-center gap-3 mb-4">
          <Skeleton variant="circle" style={{ width: 48, height: 48 }} />
          <div>
            <Skeleton style={{ width: 100, height: 16, marginBottom: 4 }} />
            <Skeleton style={{ width: 70, height: 12 }} />
          </div>
        </div>

        {/* Customer since */}
        <div className="flex items-center gap-2">
          <Skeleton style={{ width: 16, height: 16 }} />
          <Skeleton style={{ width: 140, height: 14 }} />
        </div>
      </div>

      {/* Type & Tags section */}
      <div className="p-4" style={{ borderBottom: "1px solid var(--border-primary)" }}>
        <Skeleton style={{ width: 80, height: 12, marginBottom: 12 }} />

        {/* Type badge */}
        <Skeleton style={{ width: 80, height: 26, borderRadius: 9999, marginBottom: 12 }} />

        {/* Tags */}
        <div className="flex flex-wrap gap-2">
          <Skeleton style={{ width: 60, height: 24, borderRadius: 9999 }} />
          <Skeleton style={{ width: 75, height: 24, borderRadius: 9999 }} />
          <Skeleton style={{ width: 50, height: 24, borderRadius: 9999 }} />
        </div>
      </div>

      {/* Notes Preview section */}
      <div className="p-4">
        <Skeleton style={{ width: 100, height: 12, marginBottom: 8 }} />
        <Skeleton style={{ width: "100%", height: 14, marginBottom: 4 }} />
        <Skeleton style={{ width: "90%", height: 14, marginBottom: 4 }} />
        <Skeleton style={{ width: "70%", height: 14 }} />
      </div>
    </aside>
  );
}

/** SkeletonCallHistory - Skeleton for call history section /
function SkeletonCallHistory() {
  return (
    <SkeletonCard style={{ overflow: "hidden" }}>
      {/* Header */}
      <div
        className="px-4 py-3 border-b flex items-center justify-between"
        style={{ borderColor: "var(--border-primary)" }}
      >
        <Skeleton style={{ width: 80, height: 16 }} />
        <Skeleton style={{ width: 50, height: 14 }} />
      </div>

      {/* Call entries */}
      <div className="p-4">
        {Array.from({ length: 4 }).map((_, i) => (
          <div
            key={i}
            className="flex items-center gap-4 py-3 border-b last:border-b-0"
            style={{ borderColor: "var(--border-primary)" }}
          >
            {/* Icon */}
            <Skeleton style={{ width: 32, height: 32, borderRadius: 8 }} />

            {/* Content */}
            <div className="flex-1">
              <div className="flex items-center gap-2 mb-1">
                <Skeleton style={{ width: 100, height: 14 }} />
                <Skeleton style={{ width: 70, height: 20, borderRadius: 9999 }} />
              </div>
              <Skeleton style={{ width: 180, height: 12 }} />
            </div>

            {/* Date/time */}
            <div className="text-right">
              <Skeleton style={{ width: 60, height: 14, marginLeft: "auto" }} />
              <Skeleton style={{ width: 40, height: 12, marginTop: 4, marginLeft: "auto" }} />
            </div>
          </div>
        ))}
      </div>
    </SkeletonCard>
  );
}

/** SkeletonNotesEditor - Skeleton for notes editor section /
function SkeletonNotesEditor() {
  return (
    <SkeletonCard style={{ padding: 16 }}>
      <Skeleton style={{ width: 100, height: 16, marginBottom: 12 }} />
      <Skeleton
        style={{
          width: "100%",
          height: 120,
          borderRadius: 8,
        }}
      />
    </SkeletonCard>
  );
}

/** CustomerDetailSkeleton - Full customer detail page skeleton



## Usage

```tsx
import { CustomerDetailSkeleton } from '@/components/CustomerDetailSkeleton';

export default function Example() {
  return (
    <CustomerDetailSkeleton  />
  );
}
```

---
title: "JobStatusBadge"
description: "* JobStatusBadge - Visual indicator for job ticket status * Status colors: - Open: Amber (needs attention) - Scheduled: Blue (appointment booked) - Completed: Green (work done) - Closed: Gray (archived)"
---

* JobStatusBadge - Visual indicator for job ticket status * Status colors: - Open: Amber (needs attention) - Scheduled: Blue (appointment booked) - Completed: Green (work done) - Closed: Gray (archived)

## Props

<ResponseField name="status" type="JobStatus" required={true}>
  status
</ResponseField>
<ResponseField name="size" type=""sm" | "md"" required={false}>
  size
</ResponseField>
<ResponseField name="showIcon" type="boolean" required={false}>
  showIcon
</ResponseField>


## Usage

```tsx
import { JobStatusBadge } from '@/components/JobStatusBadge';

export default function Example() {
  return (
    <JobStatusBadge status={status} />
  );
}
```

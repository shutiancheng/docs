---
title: "AgentsSkeleton"
description: "* SkeletonStatCard - Skeleton for the stat overview cards Matches the stat card layout (icon + label, large number) /
function SkeletonStatCard() {
  return (
    <SkeletonCard style={{ padding: "var(--space-4)" }}>
      {/* Icon and label row */}
      <div className="flex items-center gap-2 mb-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 90, height: 14 }} />
      </div>
      {/* Large number */}
      <Skeleton style={{ width: 60, height: 28 }} />
    </SkeletonCard>
  );
}

/** SkeletonAgentCard - Skeleton for a single agent card Matches the AgentCard component layout /
interface SkeletonAgentCardProps {
  variant?: "active" | "idle" | "disabled";
}

function SkeletonAgentCard({ variant = "active" }: SkeletonAgentCardProps) {
  return (
    <SkeletonCard
      style={{
        minHeight: 280,
        display: "flex",
        flexDirection: "column",
      }}
    >
      {/* Header */}
      <div
        className="flex items-center justify-between border-b shrink-0"
        style={{
          padding: "var(--space-4) var(--space-5)",
          borderColor: "var(--border-primary)",
        }}
      >
        <div className="flex items-center gap-3">
          {/* Bot icon box */}
          <Skeleton style={{ width: 40, height: 40, borderRadius: 8 }} />
          <div>
            {/* Agent name */}
            <Skeleton style={{ width: 100, height: 16, marginBottom: 4 }} />
            {/* Phone number */}
            <Skeleton style={{ width: 120, height: 14 }} />
          </div>
        </div>
        <div className="flex items-center gap-2">
          {/* Status indicator */}
          <div className="flex items-center gap-2">
            <Skeleton variant="circle" style={{ width: 8, height: 8 }} />
            <Skeleton style={{ width: 45, height: 14 }} />
          </div>
          {/* Menu button */}
          <Skeleton style={{ width: 24, height: 24 }} />
        </div>
      </div>

      {/* Body */}
      <div
        className="flex-1 flex flex-col"
        style={{ padding: "var(--space-4) var(--space-5)" }}
      >
        {/* Stats grid */}
        <div className="grid grid-cols-2 gap-4">
          {/* Calls Today stat */}
          <div>
            <div className="flex items-center gap-2" style={{ marginBottom: "var(--space-1)" }}>
              <Skeleton style={{ width: 12, height: 12 }} />
              <Skeleton style={{ width: 65, height: 14 }} />
            </div>
            <Skeleton style={{ width: 35, height: 22 }} />
          </div>
          {/* Avg Wait Time stat */}
          <div>
            <div className="flex items-center gap-2" style={{ marginBottom: "var(--space-1)" }}>
              <Skeleton style={{ width: 12, height: 12 }} />
              <Skeleton style={{ width: 80, height: 14 }} />
            </div>
            <Skeleton style={{ width: 40, height: 22 }} />
          </div>
        </div>

        {/* Greeting section */}
        <div
          className="mt-4 pt-4 flex-1"
          style={{ borderTop: "1px solid var(--border-primary)" }}
        >
          <div className="flex items-center gap-2 mb-2">
            <Skeleton style={{ width: 12, height: 12 }} />
            <Skeleton style={{ width: 50, height: 14 }} />
          </div>
          {/* Greeting text lines */}
          <Skeleton style={{ width: "100%", height: 16, marginBottom: 6 }} />
          <Skeleton style={{ width: "85%", height: 16, marginBottom: 6 }} />
          <Skeleton style={{ width: "60%", height: 16 }} />
        </div>

        {/* Footer */}
        <div
          className="flex items-center justify-between mt-4 pt-4 shrink-0"
          style={{ borderTop: "1px solid var(--border-primary)" }}
        >
          {/* Voice Model label */}
          <Skeleton style={{ width: 140, height: 14 }} />
          {/* Configure button */}
          <Skeleton style={{ width: 75, height: 26, borderRadius: 4 }} />
        </div>
      </div>
    </SkeletonCard>
  );
}

/** AgentsSkeleton - Full agents page skeleton Matches the exact layout of the agents page"
---

* SkeletonStatCard - Skeleton for the stat overview cards Matches the stat card layout (icon + label, large number) /
function SkeletonStatCard() {
  return (
    <SkeletonCard style={{ padding: "var(--space-4)" }}>
      {/* Icon and label row */}
      <div className="flex items-center gap-2 mb-2">
        <Skeleton style={{ width: 16, height: 16 }} />
        <Skeleton style={{ width: 90, height: 14 }} />
      </div>
      {/* Large number */}
      <Skeleton style={{ width: 60, height: 28 }} />
    </SkeletonCard>
  );
}

/** SkeletonAgentCard - Skeleton for a single agent card Matches the AgentCard component layout /
interface SkeletonAgentCardProps {
  variant?: "active" | "idle" | "disabled";
}

function SkeletonAgentCard({ variant = "active" }: SkeletonAgentCardProps) {
  return (
    <SkeletonCard
      style={{
        minHeight: 280,
        display: "flex",
        flexDirection: "column",
      }}
    >
      {/* Header */}
      <div
        className="flex items-center justify-between border-b shrink-0"
        style={{
          padding: "var(--space-4) var(--space-5)",
          borderColor: "var(--border-primary)",
        }}
      >
        <div className="flex items-center gap-3">
          {/* Bot icon box */}
          <Skeleton style={{ width: 40, height: 40, borderRadius: 8 }} />
          <div>
            {/* Agent name */}
            <Skeleton style={{ width: 100, height: 16, marginBottom: 4 }} />
            {/* Phone number */}
            <Skeleton style={{ width: 120, height: 14 }} />
          </div>
        </div>
        <div className="flex items-center gap-2">
          {/* Status indicator */}
          <div className="flex items-center gap-2">
            <Skeleton variant="circle" style={{ width: 8, height: 8 }} />
            <Skeleton style={{ width: 45, height: 14 }} />
          </div>
          {/* Menu button */}
          <Skeleton style={{ width: 24, height: 24 }} />
        </div>
      </div>

      {/* Body */}
      <div
        className="flex-1 flex flex-col"
        style={{ padding: "var(--space-4) var(--space-5)" }}
      >
        {/* Stats grid */}
        <div className="grid grid-cols-2 gap-4">
          {/* Calls Today stat */}
          <div>
            <div className="flex items-center gap-2" style={{ marginBottom: "var(--space-1)" }}>
              <Skeleton style={{ width: 12, height: 12 }} />
              <Skeleton style={{ width: 65, height: 14 }} />
            </div>
            <Skeleton style={{ width: 35, height: 22 }} />
          </div>
          {/* Avg Wait Time stat */}
          <div>
            <div className="flex items-center gap-2" style={{ marginBottom: "var(--space-1)" }}>
              <Skeleton style={{ width: 12, height: 12 }} />
              <Skeleton style={{ width: 80, height: 14 }} />
            </div>
            <Skeleton style={{ width: 40, height: 22 }} />
          </div>
        </div>

        {/* Greeting section */}
        <div
          className="mt-4 pt-4 flex-1"
          style={{ borderTop: "1px solid var(--border-primary)" }}
        >
          <div className="flex items-center gap-2 mb-2">
            <Skeleton style={{ width: 12, height: 12 }} />
            <Skeleton style={{ width: 50, height: 14 }} />
          </div>
          {/* Greeting text lines */}
          <Skeleton style={{ width: "100%", height: 16, marginBottom: 6 }} />
          <Skeleton style={{ width: "85%", height: 16, marginBottom: 6 }} />
          <Skeleton style={{ width: "60%", height: 16 }} />
        </div>

        {/* Footer */}
        <div
          className="flex items-center justify-between mt-4 pt-4 shrink-0"
          style={{ borderTop: "1px solid var(--border-primary)" }}
        >
          {/* Voice Model label */}
          <Skeleton style={{ width: 140, height: 14 }} />
          {/* Configure button */}
          <Skeleton style={{ width: 75, height: 26, borderRadius: 4 }} />
        </div>
      </div>
    </SkeletonCard>
  );
}

/** AgentsSkeleton - Full agents page skeleton Matches the exact layout of the agents page



## Usage

```tsx
import { AgentsSkeleton } from '@/components/AgentsSkeleton';

export default function Example() {
  return (
    <AgentsSkeleton  />
  );
}
```

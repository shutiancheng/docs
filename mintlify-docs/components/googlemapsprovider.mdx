---
title: "GoogleMapsProvider"
description: "* Google Maps libraries to load 'places' is required for address autocomplete /
const LIBRARIES: ("places")[] = ["places"];

interface GoogleMapsProviderProps {
  children: React.ReactNode;
}

// Context to share the loaded state
const GoogleMapsContext = React.createContext<boolean>(false);

/** Provider component that loads the Google Maps JavaScript API. Must wrap any components that use Google Places features. * Uses useJsApiLoader hook instead of LoadScript component to properly handle re-renders and prevent "google api is already presented" errors. * Requires NEXT_PUBLIC_GOOGLE_MAPS_API_KEY environment variable."
---

* Google Maps libraries to load 'places' is required for address autocomplete /
const LIBRARIES: ("places")[] = ["places"];

interface GoogleMapsProviderProps {
  children: React.ReactNode;
}

// Context to share the loaded state
const GoogleMapsContext = React.createContext<boolean>(false);

/** Provider component that loads the Google Maps JavaScript API. Must wrap any components that use Google Places features. * Uses useJsApiLoader hook instead of LoadScript component to properly handle re-renders and prevent "google api is already presented" errors. * Requires NEXT_PUBLIC_GOOGLE_MAPS_API_KEY environment variable.

## Props

<ResponseField name="children" type="React.ReactNode" required={true}>
  children
</ResponseField>


## Usage

```tsx
import { GoogleMapsProvider } from '@/components/GoogleMapsProvider';

export default function Example() {
  return (
    <GoogleMapsProvider children={children} />
  );
}
```

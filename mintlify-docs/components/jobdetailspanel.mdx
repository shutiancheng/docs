---
title: "JobDetailsPanel"
description: "Agent services config for auto-populating job pricing */
  agentServices?: AgentServicesConfig | null;
}

/** JobDetailsPanel - Sidebar panel for job ticket information * Displays job status, technician assignment, appointment details, and provides action buttons based on current status."
---

Agent services config for auto-populating job pricing */
  agentServices?: AgentServicesConfig | null;
}

/** JobDetailsPanel - Sidebar panel for job ticket information * Displays job status, technician assignment, appointment details, and provides action buttons based on current status.

## Props

<ResponseField name="job" type="Job" required={true}>
  job
</ResponseField>
<ResponseField name="onUpdate" type="() => void" required={true}>
  onUpdate
</ResponseField>
<ResponseField name="onSchedule" type="() => void" required={false}>
  onSchedule
</ResponseField>
<ResponseField name="onComplete" type="() => void" required={false}>
  onComplete
</ResponseField>
<ResponseField name="onClose" type="() => void" required={false}>
  onClose
</ResponseField>
<ResponseField name="onReopen" type="() => void" required={false}>
  onReopen
</ResponseField>
<ResponseField name="agentServices" type="AgentServicesConfig | null" required={false}>
  agentServices
</ResponseField>


## Usage

```tsx
import { JobDetailsPanel } from '@/components/JobDetailsPanel';

export default function Example() {
  return (
    <JobDetailsPanel job={job} onUpdate={onUpdate} />
  );
}
```

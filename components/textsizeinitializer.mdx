---
title: "TextSizeInitializer"
description: "* TextSizeInitializer - Client component that applies saved text size on mount Add this to your layout to persist text size across page loads"
---

* TextSizeInitializer - Client component that applies saved text size on mount Add this to your layout to persist text size across page loads



## Usage

```tsx
import { TextSizeInitializer } from '@/components/TextSizeInitializer';

export default function Example() {
  return (
    <TextSizeInitializer  />
  );
}
```

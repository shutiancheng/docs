---
title: "AudioPlayerWrapper"
description: "* AudioPlayerWrapper - Client component wrapper for audio player provider * This wrapper is needed because the dashboard layout is a server component but AudioPlayerProvider needs to be a client component."
---

* AudioPlayerWrapper - Client component wrapper for audio player provider * This wrapper is needed because the dashboard layout is a server component but AudioPlayerProvider needs to be a client component.



## Usage

```tsx
import { AudioPlayerWrapper } from '@/components/AudioPlayerWrapper';

export default function Example() {
  return (
    <AudioPlayerWrapper  />
  );
}
```

---
title: "JobEditDialog"
description: "JobEditDialog component"
---

JobEditDialog component

## Usage

```tsx
import { JobEditDialog } from '@/components/JobEditDialog';

export default function Example() {
  return (
    <JobEditDialog job={job} open={open} onOpenChange={onOpenChange} />
  );
}
```

